# 배포 후 수정 계획

> 2026-09-07, Vercel 배포본(`slate-ten-cyan.vercel.app`) 실사용 확인 + 코드베이스 전수 감사 결과.
> 확인된 문제 16건 + 배포 리전 1건. 전부 파일 근거로 반증 검증을 거친 것들.
>
> 티켓 하나 = 브랜치 하나 = 커밋 하나. 순서는 의존성과 "팀이 덜 막히는 순"으로 정렬.

---

## 배경

배포는 됐고 로그인·보드·백로그·리포트·이슈 상세·설정은 렌더된다. 네트워크 요청 43건 전부 200이고 콘솔 에러도 없다. 그럼에도 "안 되는 게 많다"고 느껴지는 이유는 세 종류다.

1. **없는 기능이 있는 척한다** — 사이드바 에픽 메뉴가 404, 에픽 필터가 항상 0건
2. **실패가 조용하다** — RLS에 막힌 UPDATE가 0행 성공으로 끝나 에러 토스트가 안 뜬다
3. **막다른 길이 있다** — 404 화면에 앱으로 돌아갈 링크가 없다

여기에 함수 실행 리전이 미국(`iad1`)이고 Supabase는 서울이라 페이지 이동마다 약 215ms를 왕복에 쓴다.

`CLAUDE.md`의 "1단계 W1~W8 전부 완성"은 사실과 다르다. 에픽 화면, 이슈 제목·본문 편집, 권한 게이팅, Realtime은 만든 적이 없다.

---

## 1차 — 지금 바로 (팀이 덜 막히게)

배포 한 번으로 체감이 가장 크게 바뀌는 묶음. 기능 추가 없이 이미 있는 것을 정상화한다.

- [x] **F-1 배포 리전 서울 고정 + 로딩 셸**
  `apps/web/vercel.json`에 `"regions": ["icn1"]` 추가. `app/loading.tsx`, `app/projects/[projectKey]/loading.tsx`, `app/i/[issueKey]/loading.tsx` 신규.
  이슈 상세도 자체 사이드바를 렌더하는 서버 컴포넌트라 셸을 같이 넣었다(감사에서 3단 워터폴로 지목된 화면).
  근거: `x-vercel-id: icn1::iad1::...` (엣지 128ms vs 함수 345ms). `app/` 트리에 `loading.tsx` 0개라 서버 왕복이 끝날 때까지 HTML이 1바이트도 안 나감.
  커밋: `perf: 함수 실행 리전 서울 고정 및 로딩 셸 추가`

- [x] **F-2 404·에러 탈출구**
  `app/not-found.tsx`(홈 링크), `app/error.tsx`(다시 시도 + 홈 링크 + digest) 신규.
  **계획에서 뺀 것**: `app/projects/[projectKey]/not-found.tsx`는 만들지 않았다. 각 페이지의 `notFound()` 조건이 레이아웃과 동일한 `if (!project)`라 레이아웃이 먼저 던지고, 레이아웃의 `notFound()`는 부모 경계가 잡는다. 즉 그 파일은 렌더될 수 없는 죽은 코드가 된다.
  검증: `/projects/DOTOLI/epics`(매칭 없음), `/projects/NOPE/board`(잘못된 키), `/i/DOTOLI-99999`(없는 이슈) 세 경로 모두 커스텀 404 + 홈 링크 동작 확인. `error.tsx`는 실제 서버 오류를 일으켜 확인하지는 않았다.
  근거: `notFound()` 호출이 6곳(`projects/[projectKey]/layout.tsx:22`, board/backlog/reports/settings `:19~25`, `i/[issueKey]/page.tsx:24`)인데 `not-found.tsx`가 없어 Next 기본 404로 떨어진다. 루트 레이아웃(`app/layout.tsx:24-35`)은 Provider와 Toaster만 렌더해서 그 화면에 링크가 0개다.
  커밋: `fix: 404·에러 화면에 앱 복귀 경로 추가`

- [x] **F-3 무음 실패 제거**
  `lib/queries/sprints.ts`의 `useStartSprint` UPDATE에 `.select("id")` 붙여 0행이면 throw. `IssueMetaPanel`의 `update.mutate` 호출에 `onError` 토스트 연결(또는 `useUpdateIssue`에 기본 `onError`).
  근거: `sprints.ts:63-70`은 `.select()` 없는 UPDATE라 RLS가 행을 가리면 `error: null` + 0행으로 끝난다. PostgREST는 차단이 아니라 은닉이다.
  **범위**: 같은 결함 계열을 전부 덮었다. 공용 헬퍼 `lib/queries/assert-written.ts` 를 만들어 `useStartSprint`, `useUpdateIssue`, `useDeleteIssue`, `useUpdateIssueStatus`, `useUpdateIssueSprint`, `useReorderIssues` 의 쓰기에 `.select("id")` + 0행 검출을 붙였다. 메타 패널은 호출부가 8곳인데 어디도 `onError` 를 안 넘겨서 `useUpdateIssue` 훅에 기본 토스트를 뒀다.
  RLS 확인: `sprints_read_all`(select, true)와 `sprints_admin_write`(all, is_admin) 조합이라 Admin 은 정상 반영 후 행이 돌아오고 멤버만 0행이 된다. 성공을 실패로 오판할 경로는 없다.
  **미검증**: 브라우저 세션이 만료돼 로그인 상태 회귀 확인은 못 했다. 정적 검사만 통과한 상태.
  커밋: `fix: RLS 차단과 변경 실패를 사용자에게 표면화`

- [x] **F-4 권한 게이팅 배선**
  `useIsAdmin()`(`lib/queries/users.ts:95`)을 실제로 호출. `BacklogView`의 시작·마감·새 스프린트 버튼을 멤버에게는 숨기거나 비활성 + 사유 표시.
  근거: `useIsAdmin()`이 구현돼 있는데 코드베이스 어디서도 호출되지 않는다. `BacklogView.tsx:204`는 role 검사 없이 모두에게 시작 버튼을 렌더한다.
  의존: F-3(에러 표면화가 먼저 있어야 게이팅 누락분이 드러남)
  **범위**: 홈(`CreateProjectDialog`)·프로젝트 설정·워크스페이스 설정은 이미 서버에서 게이팅돼 있었다. 빠진 건 `BacklogView` 뿐이라 거기만 손봤다. 시작·마감·새 스프린트를 `{isAdmin && ...}` 로 감쌌고(홈·설정과 같은 패턴), F-17 의 드래그 차단 사유도 역할별로 나눴다 — 멤버에게 "스프린트를 만들면"이라고 안내하면 만들 수 없는 일을 시키는 셈이라 "관리자가 스프린트를 만들면"으로 바꿨다.
  **알려진 깜빡임**: `useIsAdmin()` 은 사용자 조회 중 `false` 라 Admin 도 첫 렌더에 버튼이 잠깐 안 보인다. 반대 방향(멤버에게 보였다 사라짐)보다 안전한 쪽이라 그대로 뒀다.
  커밋: `fix: 스프린트 조작 버튼 Admin 게이팅 연결`

- [x] **F-5 검색 진입점 노출**
  사이드바에 `이슈 검색 ⌘K` 버튼, 도움말 `?` 아이콘 추가. `useUiStore`의 기존 오프너 재사용.
  근거: `GlobalSearchModal`은 동작하는데 화면상 진입점이 ⌘K뿐이라 존재를 알 수 없다.
  **위치**: 검색은 사이드바 상단(로고 아래)에 `이슈 검색  ⌘K` 버튼, 도움말은 하단에서 테마 토글 옆 `?` 아이콘. 표기는 기존 `ShortcutsHelpDialog` 의 `⌘` `K` 규약을 따랐다.
  **미검증**: F-4 와 함께 로그인 상태 화면 확인은 못 했다(브라우저 세션 만료). 정적 검사만 통과.
  커밋: `feat: 사이드바에 검색·도움말 진입점 추가`

---

## 1.5차 — 드래그앤드롭 (사용자 직접 보고)

코드 감사 결과 원인이 6개다. 중복 제거 후 4개 티켓으로 나눈다.

- [x] **F-15 백로그 드래그 조작성**
  `DraggableIssueRow`의 `{...attributes} {...listeners}`를 그립 버튼에서 래퍼 div로 옮기고, 그립에는 `setActivatorNodeRef`만 남긴다. `IssueRow`의 `<Link>` 2개에 `draggable={false}` 추가. 그립 히트박스를 24px 이상으로, 색을 `/40` → `/70` 이상으로. `DragOverlay`에 불투명 배경(`bg-card border shadow-lg`)을 주고 원본 행의 `transform`은 제거.
  근거: 라이브 실측으로 잡을 수 있는 면적이 16×16px 3개뿐이고 대비 1.6:1이다. 행 안의 링크 6개가 전부 `draggable: true`(네이티브 `<a>` 기본값)라 제목이나 키를 잡고 끌면 브라우저 링크 드래그가 뜨고 dnd-kit은 시작조차 안 된다. 사용자가 가장 자연스럽게 하는 조작이 정확히 이것이다.
  커밋: `fix: 백로그 행 전체를 드래그 소스로 전환 및 링크 드래그 차단`

- [x] **F-16 드롭 대상 판정 교정** (터치 제외)
  두 `DndContext`에 `collisionDetection` 지정: `pointerWithin` 우선, 결과가 비면 `rectIntersection` 폴백. `IssueCard`와 그립 버튼에 `touch-none` 추가.
  근거: 기본값이 `rectIntersection`이고 이건 포인터가 아니라 드래그 사각형의 **IoU(교집합/합집합)** 로 판정한다(`core.esm.js:2864`, `:398-414`). 대상이 작을수록 점수가 높아서 카드가 컬럼을 항상 이긴다. 목표 컬럼에 절반 넘게 밀어넣어도 원래 컬럼에 남은 부분이 그쪽 카드와 겹치면 `over.status === activeStatus`가 되어 `Board.tsx:51`에서 조용히 무시된다. `touch-action: none`이 없어 터치 기기에서는 드래그가 스크롤에 먹힌다.
  커밋: `fix: 포인터 기준 충돌 감지 및 터치 드래그 지원`
  **진행**: 충돌 감지는 백로그·보드 양쪽에 `pointerFirstCollision`으로 반영했다(`lib/dnd.ts`). `touch-none`(터치 기기 지원)은 아직 안 넣었다. 그것만 남았다.

- [x] **F-17 드롭할 곳이 없는 상태 드러내기**
  활성·예정 스프린트가 0개면 드래그 핸들을 비활성화하고 "스프린트를 먼저 만들어주세요" CTA(`CreateSprintDialog`)로 유도. 같은 존 드롭은 조용히 무시하되, 유효한 목적지가 없을 때는 안내를 준다.
  근거: 지금 DOTOLI는 활성·예정 스프린트가 0개라 등록된 드롭 존이 백로그 하나뿐이다. 라이브에서 `zoneCount: 1`로 확인했다. `BacklogView.tsx:116`의 `dragged.sprint_id === targetSprintId`에 걸려 **모든 드래그가 무음 no-op**이다. 사용자가 본 증상의 가장 직접적인 원인.
  커밋: `fix: 드롭 대상이 없을 때 드래그 비활성화 및 안내`

- [x] **F-18 보드 컬럼 내 순서 저장**
  `Column.tsx`의 `SortableContext`/`verticalListSortingStrategy`를 걷어내고 `IssueCard`를 `useSortable` → `useDraggable`로 내린다.
  근거: 같은 컬럼 안에서 카드를 옮기면 다른 카드가 자리를 비켜주는 정렬 애니메이션이 나오는데, `Board.tsx:51`이 early return이라 저장이 안 되고 손을 떼면 원위치로 튕긴다. `rank`를 UPDATE 하는 코드가 저장소 전체에 0건이다(스키마 `init_schema.sql:82`, select·order만 존재). 백로그는 `DraggableIssueRow.tsx:13-14` 주석에서 이미 같은 이유로 `useDraggable`을 택했다. PRD는 보드의 상태 변경(142행)과 백로그의 스프린트 이동(150행)만 요구하고 컬럼 내 순서 저장은 요구하지 않는다.
  **결정 변경(2026-09-08)**: 처음에는 PRD에 순서 저장 요구가 없다는 이유로 피드백만 없애려 했으나, 사용자가 순서 저장을 요구해 **구현하는 쪽으로 바꿨다**. 백로그도 `useDraggable` → `useSortable`로 올려 같은 방식으로 저장한다.
  **방식**: `issues.rank`(text)에 9자리 0 패딩 정수를 넣어 사전순과 숫자순을 일치시킨다. 드롭 시 그룹의 새 순서를 계산해 rank가 바뀌는 행만 갱신한다. 마이그레이션 없이 기존 컬럼만 쓴다. 순수 함수(`lib/issue-rank.ts`)로 분리해 단위 테스트 11개로 검증.
  커밋: `fix: 백로그·보드 드래그앤드롭 정상화 및 순서 저장 추가`

---

## 2차 — 기능 구멍 (지라 대체로서 필수)

없으면 도구를 쓸 수 없는 것들. 1차보다 작업량이 크다.

- [x] **F-6 이슈 제목·본문 편집**
  `IssueDetailView`의 제목을 인라인 편집(클릭 → Input), 설명 섹션에 편집 토글 + Textarea. 저장은 `useUpdateIssue`로 `{ title }` / `{ body_markdown }` patch.
  근거: 편집 UI가 아예 없다. `useUpdateIssue`(`lib/queries/issue-detail.ts:63-76`)는 임의 필드를 받게 돼 있는데 title·body_markdown을 넘기는 호출자가 0건. 백로그 인라인 생성은 제목만 받으므로 그렇게 만든 이슈는 설명을 영영 못 채운다. 지우고 다시 만들면 이슈 키가 바뀌어 절대원칙 1과 충돌.
  **구현**: `EditableTitle`(클릭 → Input, Enter/blur 저장, Esc 취소, 빈 제목이면 되돌림), `EditableBody`(편집 버튼 → Textarea, 저장/취소, Esc 취소). 둘 다 `useUpdateIssue` 재사용.
  **곁들여 고친 것**: `useUpdateIssue` 에 낙관적 업데이트 + 롤백을 넣었다. 없으면 저장 후 재조회까지 옛 값이 남아 화면이 깜빡인다. 메타 패널의 상태·우선순위 변경 깜빡임도 같이 사라진다.
  커밋: `feat: 이슈 제목·본문 인라인 편집`

- [x] **F-7 에픽 붙이기 (epic_id 쓰기 경로)**
  `IssueMetaPanel`에 에픽 Select 추가(같은 프로젝트의 `type='epic'` 이슈 + "없음"). `QuickCreateModal` 페이로드에 `epicId` 추가.
  근거: 앱 전체에서 `epic_id`를 쓰는 코드가 0건이다. 읽기(셀렉트, 필터 판정, 활동 로그)만 있다. 그래서 에픽 필터를 고르면 항상 0건이고, `IssueMetaPanel.tsx:207`의 에픽 링크는 렌더될 일이 없는 죽은 코드다.
  **구현**: `IssueMetaPanel` 의 읽기 전용 에픽 행을 Select 로 바꿨다. 후보는 보드·백로그가 이미 쓰는 `useProjectIssues` 캐시에서 `type === "epic"` 인 이슈를 걸러 쓴다(추가 쿼리 없음). 자기 자신과 에픽 타입 이슈는 제외 — 1단계는 1계층이다. 에픽이 붙으면 그 에픽으로 가는 링크 행이 따로 나온다.
  **효과**: 보드·백로그의 에픽 필터가 이제 실제로 걸린다(`issue-filters.ts` 는 원래 `epic_id` 로 매칭하고 있었는데 값이 항상 null 이라 0건이었다).
  **미룬 것**: `QuickCreateModal` 의 에픽 필드. 후보가 선택한 프로젝트에 따라 바뀌어 폼 배선이 늘어나는데, 생성 직후 상세에서 붙일 수 있어 급하지 않다.
  커밋: `feat: 이슈에 에픽 연결 기능`

- [x] **F-8 에픽 목록 페이지**
  `app/projects/[projectKey]/epics/page.tsx` 신규. 에픽 목록 + 소속 이슈 + 진행률(완료 수 / 전체).
  근거: 사이드바(`Sidebar.tsx:15`)가 이 경로로 링크하는데 파일이 없어 404. PRD 3.2와 화면 6이 명시한 기능인데 만든 적이 없다(git 이력에도 없음).
  의존: F-7
  **구현**: 서버 컴포넌트(리포트 페이지와 같은 형태) + `EpicsView`. 집계는 순수 함수 `lib/epic-summary.ts` 로 분리해 단위 테스트 7개로 검증(자기 참조 제외, 소속 이슈 0개 에픽 유지, 완료 수·포인트, 반올림).
  에픽 카드마다 진행률 막대 + `완료/전체 이슈 · 완료/전체 pt · %`, 그 아래 소속 이슈 목록. 조회는 `useProjectIssues` 캐시 재사용이라 추가 쿼리가 없다.
  검증: `pnpm --filter web build` 라우트 표에 `/projects/[projectKey]/epics` 가 잡히는 것으로 404 해소 확인. 로그인 화면 확인은 세션 만료로 못 했다.
  커밋: `feat: 에픽 목록 페이지`

---

## 3차 — 팀 동시 사용 + 성능

혼자 쓸 때는 안 보이고 여러 명이 붙으면 드러나는 것들.

- [x] **F-9 다른 사람의 변경 반영 (포커스 refetch)**
  `QueryProvider`의 `refetchOnWindowFocus`를 `true`로 되돌리고 주석을 현실에 맞게 수정. 탭 복귀 시 최신화한다.
  근거: `QueryProvider.tsx:12` 주석이 "Realtime 구독(W3)이 무효화를 담당하므로"인데 Realtime 코드가 코드베이스에 하나도 없다. 지금은 저장은 되는데 남의 화면이 안 바뀐다.
  **결정됨**: Realtime은 도입하지 않는다. 포커스 refetch로 끝낸다. TDD 스택 표의 Realtime 항목도 함께 정리(F-14).
  커밋: `fix: 창 포커스 시 최신 데이터 재조회`

- [ ] **F-10 이슈 상세 워터폴 제거**
  서버 컴포넌트에서 `DETAIL_SELECT`를 실행해 `initialData` 또는 `HydrationBoundary`로 전달. 최소한 서버가 아는 `issue.id`를 prop으로 넘겨 코멘트·첨부·GitHub 쿼리가 상세 응답을 안 기다리게 한다.
  근거: 서버에서 이슈를 읽고도 클라이언트가 처음부터 다시 읽는 3단 워터폴. `app/i/[issueKey]/page.tsx:15`.
  커밋: `perf: 이슈 상세 서버 데이터 하이드레이션`

- [ ] **F-11 모달 지연 로딩**
  모달 3종을 `next/dynamic`으로 감싸고, `QuickCreateModal` 내부 쿼리에 `enabled: quickCreateOpen` 적용.
  근거: 닫혀 있는 빠른생성 모달이 모든 페이지에서 쿼리 3개를 쏘고 번들도 끌고 온다(`QuickCreateModal.tsx:51`).
  커밋: `perf: 모달 지연 로딩 및 미개방 시 쿼리 차단`

- [ ] **F-12 projects 중복 조회 제거**
  프로젝트 조회를 `React.cache()`로 감싸거나 레이아웃에서 한 번만 조회해 페이지로 전달.
  근거: `projects/[projectKey]/layout.tsx:15`와 각 페이지가 같은 행을 두 번 조회한다.
  커밋: `perf: 프로젝트 조회 요청 단위 캐시`

---

## 4차 — 마무리

- [ ] **F-13 GitHub 리포 연결 UI**
  프로젝트 설정에 GitHub 리포 섹션(목록·추가·삭제, Admin 전용). 또는 이 테이블을 안 쓸 거면 `GithubLinksSection`에 빈 상태 + 웹훅 안내를 표시.
  근거: `project_github_repos` 테이블(`20260601000001_init_schema.sql:37`)을 읽는 코드조차 없다. `GithubLinksSection.tsx:26`이 링크 0건이면 섹션을 통째로 숨겨서 "기능이 없는 것"과 구분이 안 된다.
  커밋: `feat: 프로젝트 GitHub 리포 연결 관리`

- [ ] **F-14 문서 정정**
  `CLAUDE.md` "현재 상태"에서 에픽 화면·이슈 편집·권한 게이팅·Realtime을 미완으로 정정. `docs/2단계_로드맵.md` Wave 1에 반영.
  근거: "1단계 W1~W8 전부 완성"이 사실과 다르다. 완성 기록이 틀린 채로 남으면 다음 판단이 또 어긋난다.
  커밋: `docs: 1단계 완성 범위 정정 — 에픽·이슈 편집·권한·Realtime 미완`

---

## 결정된 것 (2026-09-07)

1. **F-9는 포커스 refetch로 끝낸다.** Realtime은 도입하지 않는다.
2. **에픽은 F-7·F-8로 제대로 만든다.** 숨기는 선택지는 폐기.

## 착수 순서

F-1 → F-2 → F-3 → F-4 → F-5 를 먼저 끝내고 한 번 배포한다. 여기까지가 "팀이 덜 막힌다"의 경계선이고, 새 기능을 하나도 안 만들어도 된다.

그다음 드래그앤드롭 F-17 → F-15 → F-16 → F-18. F-17을 앞에 두는 이유는 지금 배포본에서 사용자가 실제로 부딪히는 게 그것(드롭할 곳이 없음)이기 때문이다.

그 뒤 F-6(이슈 편집) → F-7(에픽 연결) → F-8(에픽 페이지) → 3차 → 4차.

---

## 별건 — 테마 토글 (완료)

- [x] **F-19 다크·라이트 테마 토글**
  사이드바 하단에 시스템/라이트/다크 3택 드롭다운(`ThemeToggle`). 기본값은 OS 설정 추종.
  근거: 사용자 요청. 2택으로 두면 한 번 누른 뒤 OS 추종으로 되돌아갈 방법이 없어 3택으로 했다.
  **기존 결정 변경**: `CLAUDE.md`가 "다크 모드 디폴트"였는데 기본값을 `system`으로 바꿨다. 해당 문서 두 줄도 같이 정정했다.
  검증: OS 라이트에서 저장값 없이 `html.light`, 다크 선택 시 OS와 무관하게 `html.dark` + `localStorage.theme="dark"`, 시스템 복귀 시 다시 `html.light`. 콘솔 경고 없음.
  커밋: `feat: 사이드바 테마 토글 추가 및 기본값 OS 추종으로 변경`

---

## 진행 중 새로 발견 (티켓화 대기)

- **완료 스프린트에 남은 이슈를 되돌릴 방법이 없다.** 마감 시 "완료 스프린트에 유지"를 고르면 그 이슈가 보드·백로그 양쪽에서 사라지고, `sprint_id`를 되돌릴 유일한 수단인 백로그 드래그도 그 행이 렌더되지 않아 쓸 수 없다. 회귀 감사에서 확인됨. 이번 변경이 만든 버그는 아니다.
- **예정 스프린트를 삭제·수정할 수 없다.** `CompleteSprintDialog`는 active 전용이라, 잘못 만든 planned 스프린트를 앱 안에서 되돌릴 방법이 없다. F-17 검증 중 발견.
